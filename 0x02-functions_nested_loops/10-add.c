#include "main.h"

/**
 * add - adds two integers and returns the result
 * @a: value to be added to b
 * @b: value to be added to a
 * Return: a + b
 */
int add(int a, int b)
{
	int c;

	c = a + b;

	return (c);
}
